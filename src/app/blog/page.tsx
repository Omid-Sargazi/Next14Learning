import { Metadata } from 'next'
import React from 'react'

export const  metadata:Metadata = {
  // title:"Blog In itself"
  title:{
    absolute:"Blog Not Itself"
  }
}

export default function Blog() {
  return (
    <div>Blog</div>
  )
}
